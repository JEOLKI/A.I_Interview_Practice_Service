<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  

<mapper namespace="com.aiinterview.chat.dao.ChatMapper">

	<select id="List" parameterType="com.aiinterview.chat.vo.ChatVO" resultType="com.aiinterview.chat.vo.ChatVO">
	select * from(
	select MSG_SQ, MSG_CONTENT, 
	<![CDATA[
        CASE WHEN TO_CHAR(SYSDATE-1, 'YYYY/MM/DD') < TO_CHAR(MSG_DATE, 'YYYY/MM/DD') THEN TO_CHAR(MSG_DATE,'HH24:MI')
            ELSE TO_CHAR(MSG_DATE,'YYYY/MM/DD') END AS MSG_DATE,
             ]]>
        MSG_SENDER, MSG_RECEIVER from message
	where msg_sender = #{msgSender} and msg_receiver = #{msgReceiver}
	union
	select MSG_SQ, MSG_CONTENT,
	<![CDATA[ 
        CASE WHEN TO_CHAR(SYSDATE-1, 'YYYY/MM/DD') < TO_CHAR(MSG_DATE, 'YYYY/MM/DD') THEN TO_CHAR(MSG_DATE,'HH24:MI')
            ELSE TO_CHAR(MSG_DATE,'YYYY/MM/DD') END AS MSG_DATE,
    ]]>       
        MSG_SENDER, MSG_RECEIVER from message
	where msg_sender = #{msgReceiver} and msg_receiver = #{msgSender})
	
	</select>
	
	<insert id="create" parameterType="com.aiinterview.chat.vo.ChatVO" >
		INSERT INTO MESSAGE VALUES(message_sq_nextval,#{msgContent},SYSDATE, 
		#{msgSender}, #{msgReceiver})
		
	</insert>
	


</mapper>



